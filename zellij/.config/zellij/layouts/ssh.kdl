// Layout for SSH sessions
// Simple layout for remote connections

layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
        children
    }

    tab name="main" focus=true {
        pane
    }
}
