// Layout for Claude Code sessions
// Single focused pane for AI-assisted development

layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
        children
    }

    tab name="claude" focus=true {
        pane
    }
}
