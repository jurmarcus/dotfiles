// Layout for code/opencode sessions
// Single tab with a main editor pane

layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
        children
    }

    tab name="editor" focus=true {
        pane
    }

    tab name="terminal" {
        pane
    }
}
