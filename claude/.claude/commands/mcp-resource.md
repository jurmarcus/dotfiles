Add a new MCP resource to this server: $ARGUMENTS

Requirements:
1. Add the resource definition with proper URI template
2. Implement the resource handler
3. Return properly formatted content (text or blob)
4. Handle errors gracefully
5. Add to the resources list handler

Follow MCP spec for resource responses (uri, mimeType, text/blob).
