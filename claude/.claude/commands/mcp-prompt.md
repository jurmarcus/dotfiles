Add a new MCP prompt to this server: $ARGUMENTS

Requirements:
1. Define the prompt with name and description
2. Add any required arguments with descriptions
3. Implement the prompt handler that returns messages
4. Use proper message format (role, content)

Follow the existing patterns. Return an array of messages that can include user/assistant roles.
