Do a cleanup sweep of the codebase.

Check for and fix:
1. **Unused code** - Dead imports, unused variables, unreachable code
2. **TODOs** - List all TODO/FIXME/HACK comments, address easy ones
3. **Formatting** - Run formatters (ruff for Python, biome for JS/TS)
4. **Type errors** - Run type checker if configured
5. **Lint warnings** - Address any linter warnings

Show a summary of what was found and fixed.
